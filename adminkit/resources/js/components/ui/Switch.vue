<script setup>
import { SwitchRoot, SwitchThumb } from 'reka-ui';
import { cn } from '@/lib/utils';

const props = defineProps({
    modelValue: { type: Boolean, default: false },
    class: { type: null, default: '' },
});
defineEmits(['update:modelValue']);
</script>

<template>
    <SwitchRoot
        :model-value="props.modelValue"
        :class="
            cn(
                'peer inline-flex h-4 w-7 shrink-0 cursor-pointer items-center rounded-full border-2 border-transparent transition-colors focus-visible:outline-none focus-visible:ring-1 focus-visible:ring-ring data-[state=checked]:bg-primary data-[state=unchecked]:bg-input',
                props.class,
            )
        "
        @update:model-value="$emit('update:modelValue', $event)"
    >
        <SwitchThumb
            class="pointer-events-none block size-3 rounded-full bg-background shadow-lg transition-transform data-[state=checked]:translate-x-3 data-[state=unchecked]:translate-x-0"
        />
    </SwitchRoot>
</template>
